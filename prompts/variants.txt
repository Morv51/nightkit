You are given one finished image-generation prompt for a 9:16 event flyer template.

Produce alternative versions of it. Each must remain inside the same design world: the same creative mechanism, the same material character, the same headline morphology, the same colour world, the same compositional logic.

Vary only these two things:

MOTIF. A different subject, pose, gesture, object or scene, within the same cultural world as the original.

COLOUR. A different distribution, weighting or accent inside the existing palette. Never introduce a colour family the original does not contain.

The layout of the ten placeholder strings is carried over. Each string keeps the zone, the orientation and the relative scale the original gives it. Within its zone a string may shift to accommodate the new motif, but it does not move to a different zone, does not change from horizontal to vertical or back, and does not change its size relative to the others. Every variant must read as the same layout carrying a different image, not as a redesign.

Everything else is carried across unchanged: the ten placeholder strings, every instruction governing them, the material description, the headline system, and all prohibitions.

Do not simplify, shorten or summarise. Each variant is a complete standalone prompt of the same length and level of detail as the original.

Separate the variants with a line containing only ---

Return nothing else.
