Write the production prompt at full length, in the same register and
formatting style as the instructions you are given.

Give each of the ten placeholder strings its own separate paragraph
with its own design reasoning. Do not merge several placeholders into
one paragraph.

End with the placeholder lock as ten separate lines, one line per
string, each stating that the string must visibly read exactly as
written.

Write every placeholder string as bare text. Never wrap a placeholder
in quotation marks, typographic quotes, brackets, markdown links or any
other punctuation, because the image model may render those characters
into the flyer.
